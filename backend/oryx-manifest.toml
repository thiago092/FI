[build]
platform = "python"
platform_version = "3.11"
command = "pip install -r requirements.txt"

[application]
type = "python"
entry_point = "app:app"

[deployment]
startup_command = "gunicorn app:app --worker-class uvicorn.workers.UvicornWorker --bind=0.0.0.0:$PORT --timeout 600" 